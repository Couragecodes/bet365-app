import { Text, View } from 'react-native';
import React from 'react';

const liveGameScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center' }}>
      <Text>liveGameScreen</Text>
    </View>
  );
};

export default liveGameScreen;
